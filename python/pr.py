# enter the fruits value in tuple

# store the record of the 3 students

student = {
 
}

for _ in range(2):
    name = input("name : ")
    values = input("value: ")
    passwords = input("password: ")

    student[name]={
        "value":values,
        "password":passwords
    }

print(student)
